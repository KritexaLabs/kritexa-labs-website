## [Phase 22] — Solutions / Live Projects Information Architecture & Full Structural Redesign

### New Pages Created

- `/industry-solutions` — New top-level Solutions sub-page: "Solutions by industry / business type". Content moved from Capabilities page.
- `/live-projects` — New top-level navigation page: "Experience what we have built". Honest status — all current projects pending verified live URLs.

### Pages Redesigned

- **Capabilities** — Redesigned to focus on technology, engineering & what we can build. Removed industry-specific content (moved to Industry Solutions). New sections: engineering-domains, proof-cta.
- **Portfolio** — Updated cross-links: added Case Studies and Live Projects CTAs.
- **Case Studies** — Updated cross-links: added Industry Solutions CTA.

### Navigation Changes

- **Desktop header**: Solutions ▼ dropdown added (Capabilities / Industry Solutions / Portfolio / Case Studies). Live Projects added as top-level link.
- **Products mega menu**: "Solutions" column renamed to "Offerings" (naming collision fix with new top-level Solutions nav item).
- **Mobile drawer**: Solutions accordion added above Products accordion. Live Projects added as top-level mobile link.
- **Footer**: Restructured — "Solutions" column group added (Capabilities / Industry Solutions / Portfolio / Case Studies). Live Projects added to Main Pages.

### JavaScript Changes

- `js/navigation.js` — Solutions dropdown open/close/toggle/Escape/outside-click. Mobile Solutions accordion. Solutions closes when Products opens and vice versa.
- `js/active-nav.js` — Added routes: `/industry-solutions` → `nl-industry-solutions`, `/live-projects` → `nl-live-projects`. Solutions parent-active logic for dropdown child pages.

### CSS Changes

- `css/header.css` — Added Solutions dropdown styles (`.nav-dropdown-wrap`, `.nav-solutions-trigger`, `.nav-solutions-dropdown`, `.nsd-item`, mobile `.mob-solutions-list`).
- `css/pages/capabilities.css` — Removed who-we-serve and industry-solutions styles (moved to industry-solutions.css). Added proof-cta styles.
- `css/pages/industry-solutions.css` — NEW: Industry Solutions page styles.
- `css/pages/live-projects.css` — NEW: Live Projects page styles (cards, status badges, live CTA for future use).

### SEO Changes

- `sitemap.xml` — Added `/industry-solutions` and `/live-projects`.
- All new pages have unique title, description, canonical, OG, Twitter/X metadata.
- Breadcrumb JSON-LD auto-generated by build.js.

### Build Result

11/11 pages — no errors — no warnings.

### Regression Results

- 1 H1 per page: ✓ (all 11 pages)
- No duplicate IDs: ✓
- No broken internal links: ✓
- All external links have rel="noopener noreferrer": ✓
- All canonical tags correct: ✓
- No fake live CTAs: ✓
- All accessibility patterns preserved: ✓

### Documentation Updated

`docs/CHANGELOG.md`, `docs/DEVELOPMENT-STAGES.md`, `docs/CURRENT-ARCHITECTURE.md`, `docs/NAVIGATION.md`, `docs/CONTENT-MAP.md`, `docs/COMPONENT-INVENTORY.md`, `docs/SEO.md`, `docs/ACCESSIBILITY.md`, `docs/RESPONSIVE.md`, `docs/PERFORMANCE.md`, `docs/ARCHITECTURE-DECISIONS.md`, `docs/PHASE22-IA-REDESIGN-REPORT.md` (new).

---


## [Phase 21] — Backend, Database, CMS & Business Platform Documentation

**Backend implementation is intentionally NOT part of Phase 21.**

### Documented

- Created `docs/FUTURE-BACKEND-ROADMAP.md`, `docs/FUTURE-DATABASE-SCHEMA.md`, `docs/FUTURE-API-ARCHITECTURE.md`, `docs/FUTURE-DEPLOYMENT-ARCHITECTURE.md`, `docs/FUTURE-WORK-ROADMAP.md`, `docs/PHASE21-DOCUMENTATION-REPORT.md`.
- Updated `docs/DEVELOPMENT-STAGES.md`, `docs/ARCHITECTURE-DECISIONS.md`, `docs/CURRENT-ARCHITECTURE.md`, `docs/SECURITY.md`, `docs/PERFORMANCE.md`, `docs/ACCESSIBILITY.md`, `docs/NAVIGATION.md` with Phase 21 notes.

### Verified

- Full repository inspected (src, components, css, js, assets, scripts, docs, legacy, package.json, all 9 pages, all forms).
- 9/9 pages build successfully after documentation phase — no code changes affecting build output.
- No backend implemented, no CMS installed, no database created, no external service connected, no fake endpoints/credentials created.

### No New Dependencies

Zero new npm packages added. Project continues at zero production dependencies.

---

## [Phase 20] — 2025-08-23 — Final Security & Technical QA

### Fixed

- **`src/sections/case-studies/faq.html`** — Removed 6 inline `onclick="toggleFaq(this)"` attributes from FAQ `<button>` elements. Replaced with `data-faq-toggle` attribute. Eliminates CSP-hostile inline event handler pattern (S2-01).
- **`src/sections/contact/faq.html`** — Removed 4 inline `onclick="toggleFaq(this)"` attributes. Same fix as above.
- **`js/global.js`** — Updated `toggleFaq` to use `btn.closest('.faq-item')` instead of `btn.parentElement` for robustness. Added document-level click delegation on `[data-faq-toggle]` to dispatch `toggleFaq`. Legacy `toggleFaq` global preserved for backward-compatibility with cached pages.

### Verified

- 9/9 pages generate cleanly after all fixes.
- Zero inline `onclick=` handlers remaining in any production page (Python comment-stripped scan confirmed).
- Phase 17 SEO work intact: canonical, sitemap, robots.txt, JSON-LD, OG tags, Twitter tags, favicon — all present.
- Phase 18 accessibility work intact: focus-visible, aria-expanded, aria-controls, aria-pressed, form labels, skip navigation, keyboard navigation, reduced-motion — all verified.
- Phase 19 compatibility work intact: webkit-backdrop-filter prefixes, Firefox scrollbar styling — unchanged.
- Phase 16 performance work intact: no base64 blobs, no duplicate fonts, no RAF leaks, no new dependencies.
- No secrets or credentials found in any file.
- No localhost/filesystem/vscode-webview URLs in any production page.
- All `target="_blank"` links carry `rel="noopener noreferrer"`.
- Zero production npm dependencies — zero supply chain risk.
- No `eval()`, `new Function()`, `document.write()`, or source maps.

### Documented

- Created `docs/PHASE20-SECURITY-TECHNICAL-QA-REPORT.md` — full security audit report (34 sections).
- Created `docs/SECURITY.md` — standing security documentation.
- Updated `docs/CHANGELOG.md`, `docs/DEVELOPMENT-STAGES.md`, `docs/ARCHITECTURE-DECISIONS.md`, `docs/ACCESSIBILITY.md`, `docs/SEO.md`, `docs/NAVIGATION.md`, `docs/PERFORMANCE.md`.

### No New Dependencies

Zero new npm packages added. Project continues at zero production dependencies.

---


## [Phase 19] — 2025 — Cross-Browser & Compatibility QA

### Fixed
- **`css/components.css`**: Added `-webkit-backdrop-filter` prefix to 11 `backdrop-filter` declarations that were missing the Safari/WebKit vendor prefix. Fixes backdrop-blur rendering on Safari for hero chips, case-study card indicators, solution badges, about-page kickers and badges, contact kickers and badges, capabilities solution CTA overlay, and portfolio card badges.
- **`css/pages/portfolio.css`**: Added `-webkit-backdrop-filter` prefix to 2 `backdrop-filter` declarations (`pf-img-num`, `pf-cat-badge`).
- **`css/components.css`**: Corrected order of one `backdrop-filter`/`-webkit-backdrop-filter` pair in the footer box rule (vendor-prefixed declaration now appears before the unprefixed one, per CSS spec recommendation).
- **`css/global.css`**: Added `scrollbar-width: thin` and `scrollbar-color` to the `*` selector for Firefox-native thin purple scrollbar styling, matching the existing `-webkit-scrollbar` rules.

### Verified
- 9/9 pages generate cleanly after all fixes.
- All Phase 17 SEO meta tags intact (canonical, OG, Twitter, JSON-LD).
- All Phase 18 accessibility improvements intact (focus-visible, aria-*, skip nav, reduced-motion).
- No new dependencies added.

### Documented
- Created `docs/PHASE19-COMPATIBILITY-REPORT.md` — full compatibility audit report.
- Updated `docs/RESPONSIVE.md`, `docs/NAVIGATION.md`, `docs/ANIMATIONS.md`, `docs/ACCESSIBILITY.md`, `docs/PERFORMANCE.md`, `docs/ARCHITECTURE-DECISIONS.md`, `docs/DEVELOPMENT-STAGES.md`.

---


# Changelog — Kritexa Labs Website

All notable changes to the Kritexa Labs website rebuild project will be documented in this file. This project adheres to a strict multi-phase reconstruction plan.

## [Phase 18] — Accessibility Engineering & WCAG 2.2 AA Audit

### Added

- **`js/pages/case-studies.js`** — Vanilla JS event-delegation filter handler for case study filter buttons. Fixes P1 issue where `aria-pressed` toggle controls had no functional effect.
- **`docs/PHASE18-ACCESSIBILITY-REPORT.md`** — Full accessibility audit with WCAG 2.2 AA mapping, keyboard audit, screen reader static analysis, and bug registry.

### Fixed

- **`css/components.css`** — Added `:focus-visible` outline rules to `.fg input/select/textarea` (×2 instances) and `.kai-wl-input`. These form inputs had `outline:none` without an explicit `:focus-visible` outline fallback — WCAG 1.4.11/2.4.7 concern.
- **`css/pages/case-studies.css`** — Separated `.cs-filter-btn:hover` and `.cs-filter-btn:focus-visible` combined rule; removed `outline:none` from hover state; added `.cs-card[hidden] { display:none }` for hidden filtered cards.
- **`capabilities/index.html`** + **`src/sections/capabilities/industry-solutions.html`** — Changed 8× `<h2 class="sol-h">` to `<h3 class="sol-h">`. These headings were incorrectly at H2 level (siblings of section H2) — WCAG 1.3.1 / 2.4.6 concern.
- **`case-studies/index.html`** + **`src/sections/case-studies/studies.html`** — Added `data-category` to all 6 `.cs-card` articles for filter targeting.
- **`scripts/build.js`** — Added `js: ['/js/pages/case-studies.js']` to case-studies page definition.

### Not Changed

- Visual design — zero CSS visual changes, zero layout changes
- Navigation — zero changes to header, footer, mega menu
- Color system — no token changes required (all contrast ratios pass)
- SEO — no structural changes
- No new npm dependencies (zero dependency policy maintained)

---

## [Phase 17] — Advanced SEO, Indexing & Structured Data Engineering

### Added

- **`sitemap.xml`** — Created at project root with 9 canonical production URLs. No `/products`, no legacy/docs/src/components URLs. Priorities assigned by page importance.
- **`docs/PHASE17-SEO-REPORT.md`** — Full Phase 17 SEO audit, decisions, and validation report.
- **`meta name="robots"`** — `<meta name="robots" content="index, follow">` added to all 9 pages via `scripts/build.js`.
- **`og:image`** — Open Graph image added to all 9 pages using appropriate existing assets.
- **`og:image:width` / `og:image:height`** — Dimension hints added alongside `og:image`.
- **`og:site_name`** — `Kritexa Labs` added to all 9 pages.
- **Twitter/X Card metadata** — `twitter:card`, `twitter:site`, `twitter:title`, `twitter:description`, `twitter:image` added to all 9 pages via `scripts/build.js`.
- **Favicon** — `<link rel="icon" type="image/png" href="/assets/logos/logo.png">` added to all 9 pages.
- **`apple-touch-icon`** — `<link rel="apple-touch-icon">` added to all 9 pages.
- **`theme-color`** — `<meta name="theme-color" content="#0a0a0f">` added to all 9 pages.
- **JSON-LD structured data** — `WebSite` schema on all pages; `Organization` schema on Home and About; `WebPage` + `OfferCatalog` on Capabilities; `BreadcrumbList` on all 8 inner pages.
- **Build-time SEO file verification** — `scripts/build.js` now verifies `robots.txt` and `sitemap.xml` exist at project root and logs their sizes.

### Fixed

- **`<main id="main-content">`** — Added missing `id="main-content"` to `<main>` in `src/pages/portfolio.html`, `src/pages/case-studies.html`, and `src/pages/contact.html` source templates. These pages previously relied on the `wrapMainContent()` fallback in `scripts/build.js`; now they have it explicitly in source.

### Changed

- **`robots.txt`** — Sitemap directive activated: `Sitemap: https://www.kritexalabs.com/sitemap.xml`. Added `/qa-screenshots/` and `/components/` to Disallow. Removed "Phase 17 placeholder" comment.
- **`scripts/build.js`** — Phase label updated to Phase 17. `buildPageWrapper()` signature extended with `ogImage` and `schemaFn` parameters. SEO helper functions added: `buildOrganizationSchema()`, `buildServiceSchema()`, `buildWebSiteSchema()`, `buildBreadcrumbSchema()`. Page definitions updated with `ogImage` and `schema` properties. `SITE_URL` and `OG_DEFAULT_IMAGE` constants extracted.

### Schema Decisions Documented

- `LocalBusiness` deferred — address not independently verified
- `AggregateRating`/`Review` not added — no verifiable review data; "5.0★ Client Rating" is marketing copy
- `FAQPage` not added — Contact FAQ is a service aid, not a structured FAQ for schema
- `SearchAction` not added — no site search endpoint exists

### Not Changed

- Page titles — all 9 already correct and unique; no changes needed
- Meta descriptions — all 9 already correct and unique; no changes needed
- Canonical URLs — all already correct; no changes needed
- Visual design — zero changes to CSS, HTML structure, or content appearance
- Navigation — zero changes to header, footer, mega menu
- JavaScript — zero new JS; no changes to existing JS
- No new npm dependencies added (zero dependencies maintained)

---


---

## [Phase 16] — Performance Engineering & Core Web Vitals

### Fixed

- **P1 — CSS Payload** — `css/components.css` contained 2 JPEG base64 blobs (~394 KB total): `.about-fs-hero-bg` (~230 KB) and `.con-hero-bg` (~164 KB). Both removed and replaced with external image URLs (`/assets/images/about-hero.jpg`, `/assets/images/contact-hero.jpg`). Both images were already extracted as external files in Phase 1/7/11. Components.css reduced from 513 KB to 120 KB (-77%).
- **P2 — Google Fonts double-load** — `css/global.css` contained `@import url('https://fonts.googleapis.com/...')` in addition to the `<link>` tag already emitted by the build system in every page `<head>`. This caused Google Fonts to be requested twice per page. `@import` removed from `global.css`.
- **P3 — Dead CSS** — `css/global.css` contained `.page { display: none }` and `.page.active { display: block }` from the legacy SPA routing system. No production page uses these classes. Removed after confirming absence across all 9 built pages.

### Improved

- **LCP signal — Capabilities** — `solution-01.jpg` (above-fold LCP candidate): added `fetchpriority="high"` and `decoding="async"`.
- **LCP signal — Portfolio** — `portfolio-placeholder.jpg` card 01 (above-fold LCP candidate): added `fetchpriority="high"` and `decoding="async"`.
- **Decode performance** — `portfolio-placeholder.jpg` card 02: added `decoding="async"`.
- **Hero image preload — About** — `<link rel="preload">` for `about-hero.jpg` injected in `<head>` of about page. CSS background images are invisible to the browser preload scanner until CSS is parsed — preload hint enables earlier fetch.
- **Hero image preload — Contact** — `<link rel="preload">` for `contact-hero.jpg` injected in `<head>` of contact page. Same rationale.
- **Resource hints** — `dns-prefetch` added alongside `preconnect` for `fonts.googleapis.com` and `fonts.gstatic.com` as belt-and-suspenders for older browsers.

### Added

- `docs/PHASE16-PERFORMANCE-REPORT.md` — Full Phase 16 baseline, audit, before/after, and acceptance criteria report.
- `preloads` support in `scripts/build.js` — `buildPageWrapper()` now accepts an optional `preloads` array; emits `<link rel="preload">` hints for pages that require them.

### Changed

- `css/components.css` — `.about-fs-hero-bg`: base64 JPEG replaced with `url('/assets/images/about-hero.jpg')`. `.con-hero-bg`: base64 JPEG replaced with `url('/assets/images/contact-hero.jpg')`.
- `css/global.css` — `@import` for Google Fonts removed (replaced by comment explaining the HTML `<link>` strategy). `.page`/`.page.active` dead CSS removed.
- `css/pages/about.css` — Removed `!important` from `.about-fs-hero-bg` image override (no longer needed since `components.css` now has the correct URL). Updated comment.
- `css/pages/contact.css` — Updated comment for `.con-hero-bg` override.
- `scripts/build.js` — Added `preloads` parameter to `buildPageWrapper()`; added `dns-prefetch` hints; added `preloads` entries for `about` and `contact` page definitions.
- `src/sections/capabilities/industry-solutions.html` — `solution-01.jpg` `<img>` gained `fetchpriority="high"` and `decoding="async"`.
- `src/sections/portfolio/projects.html` — Card 01 gained `fetchpriority="high"` + `decoding="async"`. Card 02 gained `decoding="async"`.

### Not Changed

- No pages redesigned
- No visual changes
- No URLs changed
- No new npm dependencies
- No JS files changed
- No images converted/replaced/resized
- All fonts and typography preserved
- Phase 15 JS lifecycle optimizations fully preserved
- Legacy file `legacy/index-original.html` untouched

### Architecture Decisions

- **AD-057** — Base64 JPEG removal from `components.css`: Safe because both pages that reference these images already had CSS overrides in their page-specific stylesheets. The base64 was dead weight loaded on all 9 pages even though only 1 page each used it.
- **AD-058** — `preload` hints only for CSS background images: `<img>` elements are discoverable by the browser's preload scanner during HTML parsing. CSS background images are not. Preloads were therefore only added for hero background images, not for `<img>` tags.
- **AD-059** — WebP/AVIF conversion not performed: Phase 16 rules require visual verification before format conversion. Images were extracted from the original legacy source and have not been independently audited for conversion quality. Deferred to post-Phase 16.

---


## [Phase 15] — JavaScript & Animation Engineering

### Fixed

- **P0 BUG** — `js/animations.js` BUSINESS canvas IIFE ran on home page simultaneously with `js/pages/home.js` canvas IIFE, creating two competing `requestAnimationFrame` render loops on the same canvas. Fixed by adding `data-page="home"` guard in `js/animations.js`.
- **BUG** — `js/animations.js` countdown `setInterval(updateCountdown, 1000)` started on all 9 pages unconditionally. Now guarded by element existence check — only starts on `/kritexa-ai` where countdown elements are present.
- **BUG** — `js/animations.js` process `setInterval(nextPulse, 1000)` + `pulseCen` RAF started on all 9 pages via `.proc-c` query. Elements only exist on home page. Added element guard + `data-page` guard.
- **BUG** — `js/animations.js` process IIFE on home page duplicated the `js/pages/home.js` process interval. Home page was running 2× `setInterval(nextPulse, 1000)` + 2× `pulseCen` RAF. Fixed by `data-page` guard.
- **BUG** — `pulseCen` RAF loop ran continuously on desktop where `.proc-mobile` is `display:none` (element never visible). Now gated by `IntersectionObserver` — RAF only starts when `#porbCenter` is visible, stops when it leaves viewport.
- **BUG** — Countdown `setInterval` in `js/animations.js` never cleared after launch date reached. Now stored as `cdInterval` and cleared with `clearInterval(cdInterval)` when `diff <= 0`.
- **MISSING** — `mousemove` listener in `js/global.js` was missing `{ passive: true }`. Added.

### Improved

- **Cursor RAF lifecycle** — Added `visibilitychange` pause/resume. Cursor lerp calculation now stops when tab is hidden.
- **Canvas RAF lifecycle** — Added `visibilitychange` pause/resume in `js/pages/home.js`. Canvas render loop stops when tab is hidden.
- **Process interval lifecycle** — Added `visibilitychange` pause/resume in `js/pages/home.js`. Process circle pulse interval clears when tab is hidden.
- **Cursor hover listeners** — Replaced N×2 per-element `mouseenter`/`mouseleave` with 2 delegated `pointerover`/`pointerout` on `document`, filtered by `e.pointerType === 'mouse'`.
- **Cursor reduced-motion** — Added JS early return before RAF starts. Previously, CSS hid the elements but the RAF loop still ran. Now the loop never starts under `prefers-reduced-motion: reduce`.
- **Social icon hover** — Wrapped in IIFE with per-class element existence guard. No-ops if social icons are not present on the page.
- **DPR cap on animations.js canvas** — Added `Math.min(window.devicePixelRatio || 1, 2)` cap (was already capped in home.js; now consistent in the non-home fallback path too).

### Added

- `data-page` attribute on `<body>` — injected by `scripts/build.js` for every page (e.g. `<body data-page="home">`). Used by JS guards to identify the current page without URL parsing.
- `docs/PHASE15-JS-ANIMATION-REPORT.md` — Full JS/animation audit report.

### Changed

- `js/animations.js` — BUSINESS canvas guard, countdown element guard, process element guard + data-page guard, setInterval lifecycle management, countdown auto-clear, DPR cap.
- `js/global.js` — Cursor: reduced-motion guard, passive mousemove, visibilitychange pause/resume, event delegation for hover; social: element guard per class.
- `js/pages/home.js` — `pulseCen`: IntersectionObserver visibility gate; process interval: `visibilitychange` pause/resume.
- `scripts/build.js` — `buildPageWrapper()` now accepts `pageId` parameter and emits `data-page="${pageId}"` on `<body>`.
- `docs/ANIMATIONS.md`, `docs/PERFORMANCE.md`, `docs/ARCHITECTURE-DECISIONS.md`, `docs/DEVELOPMENT-STAGES.md` — Updated with Phase 15 findings.

### Not Changed

- No pages redesigned
- No visual changes
- No URLs changed
- No new npm dependencies
- No new frameworks or libraries
- `js/navigation.js` — Audited: navigation behavior preserved exactly. No changes needed.
- `js/active-nav.js` — Audited: still required for mobile nav active state + navbar.scrolled. No changes.
- `js/pages/portfolio.js` — Audited: event delegation already used, reduced-motion handled. No changes.
- All CSS files — Reduced-motion coverage confirmed comprehensive. No changes.

### Architecture Decisions

- **AD-030 RESOLVED** — The deferred Phase 6 guard (double canvas RAF on home page) is now implemented. See `docs/ARCHITECTURE-DECISIONS.md` AD-030 and `docs/PHASE15-JS-ANIMATION-REPORT.md`.
- `data-page` attribute chosen over `<meta name="page-id">` — body attribute is synchronously readable by inline scripts and requires no `querySelector` call.

---




## [Phase 14] — Real Browser, Responsive & Visual QA

### Fixed

- **QA-001 (P2)** — Verified native browser validation on contact inquiry form. Forms intercept empty/missing entries correctly since `novalidate` is removed. (QA-001 VERIFIED).
- **QA-002 (P3)** — FAQ Accordion focus and accessibility state synchronization. Modified `toggleFaq(btn)` in `js/global.js` to dynamically sync `aria-expanded` attributes with open/close visual states.

### Added

- `docs/PHASE14-QA-REPORT.md` — Completed the real browser QA portion with full results. Added Chrome/Firefox verification tables, console, networks, and keyboard QA results.
- Automated screenshot archive created in `qa-screenshots/` using the system's Google Chrome 151 and Firefox 154 in headless screenshot mode, documenting high-fidelity visual layouts.

### Changed

- `docs/DEVELOPMENT-STAGES.md` — Updated Phase 14 status from "Partially Complete" to "Complete".
- `docs/RESPONSIVE.md` / `docs/PERFORMANCE.md` / `docs/ACCESSIBILITY.md` / `docs/ANIMATIONS.md` / `docs/NAVIGATION.md` — Updated all documents with real browser manual QA outcomes.

### Not Changed

- No pages redesigned (kept all original content and grid assets)
- No new features added
- No dependencies added (no Playwright, no Puppeteer, no extra HTTP servers)
- No CMS, analytics, or backend added

---


## [Phase 13] — Career & Labs Pages Rebuild

### Added

- Created `src/sections/career/` directory — 4 section partial HTML files:
  - `hero.html` — Inner page hero (.iph), eyebrow "Join Our Team", H1 "Build the Future With Us", mailto CTA
  - `culture.html` — 6 benefit cards grid (.ben-grid/.ben-card) — "Life at Kritexa Labs"
  - `opportunities.html` — BDE job card (.job-card) with disabled apply button + email resume link
  - `cta.html` — Internship program card (.intern-card) with /contact and /about CTAs
- Created `src/sections/labs/` directory — 3 section partial HTML files:
  - `hero.html` — Inner page hero (.iph), cyan eyebrow "✦ Kritexa Labs", H1 "LABS — What We're Building"
  - `experiments.html` — Live status board, 3 active project cards with progress bars, 4 planned cards
  - `cta.html` — Newsletter "Stay in the Loop" form with honest disclaimer
- Created `src/pages/career.html` — Phase 13 composition template (replaces Phase 1 extract)
- Created `src/pages/labs.html` — Phase 13 composition template (replaces Phase 1 extract)
- Created `css/pages/career.css` — Career-page-specific styles (~6.4 KB)
- Created `css/pages/labs.css` — Labs-page-specific styles (~9.2 KB)
- Generated `career/index.html` (53.7 KB) via build system
- Generated `labs/index.html` (52.1 KB) via build system

### Changed

- `scripts/build.js` — Career and Labs page definitions updated:
  - Career: title updated to "Career — Kritexa Labs | Join Our Team"
  - Career: description updated to reflect actual page content
  - Career: `css: ['/css/pages/career.css']` added
  - Labs: title updated to "Labs — Kritexa Labs | Live Research & Experiments"
  - Labs: description updated to reflect actual page content
  - Labs: `css: ['/css/pages/labs.css']` added
  - Build system version comment updated to "Phase 13 Build System"

### Architecture Decisions

- Career and Labs remain FOOTER-ONLY pages (AD-012 preserved)
- Career does NOT appear in primary desktop header navigation (confirmed)
- Labs does NOT appear in primary desktop header navigation (confirmed)
- Career NOT added to Products mega menu (confirmed)
- Labs NOT added to Products mega menu (confirmed)
- Career accessible via: footer "Our Work" column + mobile drawer
- Labs accessible via: footer "Our Work" column + mobile drawer

### Navigation Verified Unchanged

Primary header navigation confirmed unchanged on both pages:
- Home | About | Capabilities | Portfolio | Case Studies | Contact | Products ▾ | ● Kritexa.AI
- Career and Labs appear only in mobile drawer and footer

### JavaScript

**ZERO** page-specific JavaScript files created for Career or Labs.
- No `js/pages/career.js`
- No `js/pages/labs.js`
- All interactions use existing global scripts

### Base64 Cleanup

- Career page: Zero base64 content in generated HTML (no raster images used)
- Labs page: Zero base64 content in generated HTML (no raster images used)
- No new images extracted (pages are CSS-only visual presentation)

### Legacy onclick Removed

- Career: Internship card `onclick="go('contact')"` and `onclick="go('about')"` replaced with real `<a href>` links
- Labs: Newsletter button `onclick="alert(...)"` replaced with native `<form action="#">`
- No fake success state. Honest disclaimer added to Labs newsletter form.

### Performance

| Page | Size | CSS | JS | Base64 |
|---|---|---|---|---|
| career/index.html | 53.7 KB | ~6.4 KB | 0 KB | 0 KB |
| labs/index.html | 52.1 KB | ~9.2 KB | 0 KB | 0 KB |

### Content Flags (Preserved from Legacy — Not Invented)

Career:
- "● Actively Hiring" badge — legacy source claim, status cannot be verified independently
- Apply Now button is DISABLED (preserved from legacy source — paused applications)
- Stipend ₹5,000–₹15,000/month — legacy source claim, requires Phase 21 verification
- PPO opportunity — legacy source marketing claim
- Revenue sharing, performance bonuses — legacy source marketing claims
- Compensation listed as "Performance Based" (no specific salary range)

Labs:
- "90%+ accuracy without human intervention" — unverified technical claim (preserved from legacy)
- "sub-1KB pages that load in under 0.3 seconds on 2G" — unverified claim (preserved from legacy)
- Progress percentages (73%, 61%, 48%) — aspirational from legacy source
- ETAs (Aug/Jul/Sep 2026) — aspirational from legacy source
- "Last updated: June 2026" — hardcoded date, will become outdated
- Coming soon timelines (Q3 2026 – Q1 2027) — aspirational from legacy source

### Known Deferred Items

- Real device responsive QA — Phase 14
- Career Open Graph image — Phase 17
- Labs Open Graph image — Phase 17
- Career structured data (JobPosting) — Phase 17 (requires verified active vacancy)
- Labs structured data (ResearchProject) — Phase 17 (requires verified research claims)
- Resume upload backend — Phase 21
- Newsletter real endpoint — Phase 21
- Labs status board date update — Phase 21
- Stipend range verification — Phase 21
- Career job status verification — Phase 21

---


## [Phase 12] — Kritexa.AI Page Rebuild

### Added

- Created `src/sections/kritexa-ai/` directory — 4 section partial HTML files:
  - `hero.html` — Cyan/purple hero (orbs, grid, badge group, H1, countdown timer, waitlist form)
  - `features.html` — "Three Pillars of Kritexa.AI" (3 feature card articles with progress bars)
  - `stats.html` — 4-stat horizontal strip (10M+, 8+, Q3'26, Free)
  - `cta.html` — Page-level CTA ("Be First to Experience Kritexa.AI" + Talk to Our Team + View LABS)
- Created `css/pages/kritexa-ai.css` — Kritexa.AI-page-specific styles (~4.7 KB):
  - `.kai-wl-disclaimer` — honest waitlist notice (backend not connected)
  - `.kai-wl-inner` — form wrapper for accessibility grouping
  - `.kai-cta-section` / `.kai-cta-inner` / `.kai-cta-btns` — scoped page CTA styles
  - Focus-visible overrides for `.kai-wl-input` and `.kai-wl-btn`
  - Responsive: CTA buttons stack at ≤768px; adjustments at ≤480px and ≤360px
  - Reduced motion: ALL Kritexa.AI CSS animations disabled (orb drift, blink, shimmer, progress bar fill, hover transitions)
- Rewrote `src/pages/kritexa-ai.html` — replaced Phase 1 monolithic content with section composition template (`{{SECTION:kritexa-ai/*}}`)
- Updated `scripts/build.js`:
  - Version comment updated: "Phase 12 Build System"
  - `kritexa-ai` page definition: title, description updated; `css` array added (`/css/pages/kritexa-ai.css`)

### Changed

- `src/pages/kritexa-ai.html` — Replaced Phase 1 extract (legacy monolithic content with `onclick="kaiJoin()"` and inline CSS) with clean section partial composition
- `scripts/build.js` — Build system version comment updated to Phase 12; kritexa-ai page title/description/css array updated

### Navigation Fixes (Kritexa.AI page)

- `nl-kritexaai` nav link correctly receives `class="active"` + `aria-current="page"` via `scripts/build.js` `injectActiveNav()`
- Mobile drawer `ml-kritexaai` is not manipulated by build.js (active-nav.js handles this at runtime)

### Architecture Decisions

- AD-052: Kritexa.AI Section Partial Architecture (4 sections: hero, features, stats, cta)
- AD-053: Kritexa.AI Waitlist Form — No Backend (form action="#", honest disclaimer shown, no fake success, no onclick/alert)
- AD-054: Kritexa.AI — Zero Page-Specific JavaScript (countdown driven by existing global js/animations.js IIFE)
- AD-055: Kritexa.AI Progress Bar Semantics — role="meter" with aria-valuenow/min/max
- AD-056: Kritexa.AI Feature Tags — Changed from <span> to <ul>/<li> for semantic list markup

### JavaScript

**ZERO** page-specific JavaScript created for Kritexa.AI.
Countdown timer: handled by existing global `js/animations.js` IIFE (already targets `kaiDays`, `kaiHrs`, `kaiMins`, `kaiSecs` IDs).
Waitlist form: `<form action="#">` with `type="submit"` button — no submit handler, no alert(), honest disclaimer shown.
Old `onclick="kaiJoin()"` removed — was a fake alert() with no backend.

### Base64 Cleanup

The Kritexa.AI hero uses only CSS gradients and an inline SVG-based grid background via `css-image`. No raster images on this page. **Zero base64 blobs in generated `kritexa-ai/index.html`**.

The `.kai-orb` elements and `.kai-grid` use pure CSS — no image extraction was needed.

### Content Claims Documentation

All content preserved verbatim from `legacy/index-original.html` #page-kritexaai. No AI capabilities, integrations, or metrics were added in Phase 12.

| Claim | Source | Classification |
|---|---|---|
| "The Future of Intelligent Business" | Legacy source | Marketing positioning |
| "India's most powerful AI platform" | Legacy source | Marketing claim |
| "Autonomous AI Agents" | Legacy source | Product capability claim (partially documented) |
| "68% Complete" | Legacy source | Internal status (marketing claim) |
| "ETA Q3 2026" | Legacy source | Aspirational timeline |
| "Multi-Agent Orchestration" | Legacy source | Marketing term |
| "Hinglish NLP" | Legacy source | Marketing claim |
| "WhatsApp Native" | Legacy source | Integration claim (not verified) |
| "Conversational Intelligence" | Legacy source | Product capability claim |
| "45% Complete" | Legacy source | Internal status (marketing claim) |
| "Fine-tuned LLM" | Legacy source | Marketing term (model not identified) |
| "Context Memory" | Legacy source | Marketing claim |
| "10+ Languages" | Legacy source | Aspirational claim |
| "Predictive Growth Engine" | Legacy source | Planned product claim |
| "15% Complete" | Legacy source | Internal status (marketing claim) |
| "10M+ Training Conversations" | Legacy source | ⚠️ HIGH-RISK aspirational claim |
| "8+ Indian Languages" | Legacy source | Marketing claim |
| "Q3'26 Beta Launch" | Legacy source | Partially documented (timeline) |
| "Free Early Access Tier" | Legacy source | Marketing claim (pricing not confirmed) |
| "lifetime discounts" (CTA) | Legacy source | Marketing claim (unconfirmed) |
| "priority onboarding" (CTA) | Legacy source | Marketing claim |
| "direct input into roadmap" (CTA) | Legacy source | Marketing claim |

### Performance

| Metric | Value |
|---|---|
| Legacy Kritexa.AI size | ~4.22 MB (part of monolithic index.html) |
| `kritexa-ai/index.html` | **54.6 KB** |
| `css/pages/kritexa-ai.css` | **~4.7 KB** |
| `js/pages/kritexa-ai.js` | **0 KB** (NONE) |
| Base64 removed from HTML | 0 KB (no images used on this page — pure CSS/SVG) |
| External images referenced | 0 (Kritexa.AI page uses no raster images) |
| Scripts on page | 4 (global, navigation, animations, active-nav) |

---


## [Phase 11] — Contact Page Rebuild

### Added

- Created `src/sections/contact/` directory — 4 section partial HTML files:
  - `hero.html` — Full-width image hero (kicker, H1 "Let's Build Something Remarkable Together", description, stats strip, floating badges)
  - `contact-form.html` — Two-column layout: contact options left (.ci) + project inquiry form right (.cf)
  - `faq.html` — 4 FAQ accordion items from approved legacy source
  - `cta.html` — Bottom CTA section (H2 + 2 CTAs → WhatsApp placeholder, `/portfolio`)
- Created `css/pages/contact.css` — Contact-page-specific styles (9.4 KB):
  - `.contact-hero-section .con-hero-bg` — overrides base64 blob in `css/components.css` with external image URL `/assets/images/contact-hero.jpg`
  - `.contact-eyebrow` — scoped eyebrow badge for form section
  - `.contact-optional` — optional field label variant
  - `.contact-form-notice` — honest "backend not connected" notice below form submit
  - `.contact-cta-section` / `.contact-cta-inner` / `.contact-cta-btns` — bottom CTA section
  - `.contact-faq-section .faq-wrap` — max-width constraint for FAQ items
  - Responsive: CTA buttons stack at ≤768px; adjustments at 480px and 360px
  - Reduced motion: transitions disabled for contact-specific interactive elements
- Rewrote `src/pages/contact.html` — replaced Phase 1 monolithic content with section composition template (`{{SECTION:contact/*}}`)
- Updated `scripts/build.js`:
  - Version comment updated: "Phase 11 Build System"
  - `contact` page definition: title, description updated; `css` array added (`/css/pages/contact.css`)

### Changed

- `src/pages/contact.html` — Replaced Phase 1 extract (legacy monolithic content with inline styles, onclick="submitForm()") with clean section partial composition
- `scripts/build.js` — Build system version comment updated to Phase 11; contact page `css` array added

### Architecture Decisions

- AD-048: Contact Section Partial Architecture (4 sections: hero, contact-form, faq, cta)
- AD-049: Contact Form — No Backend (form action="#", honest notice shown to users, no fake success)
- AD-050: Contact Hero Image — External Asset Override (CSS specificity override in contact.css loads external image, removing effective use of base64 in components.css for this page)
- AD-051: Contact Form — No Page-Specific JS (FAQ handled by global.js, form submission uses native HTML form with action="#")

### Navigation Fixes (Contact page)

- `nl-contact` nav link correctly receives `class="active"` + `aria-current="page"` via `scripts/build.js` `injectActiveNav()`
- Mobile drawer `ml-contact` is not manipulated by build.js (active-nav.js handles this at runtime)

### JavaScript

**ZERO** page-specific JavaScript created for Contact.
FAQ accordion: handled by existing `js/global.js` `toggleFaq()` function.
Form: `<form action="#">` with `type="submit"` button — no submit handler, no alert(), honest notice shown.
Old `onclick="submitForm()"` removed — was a fake alert() with no backend.

### Base64 Extraction (Contact Hero)

The legacy `css/components.css` contains a large base64 JPEG blob (~90 KB base64 text) in `.con-hero-bg`. 
Phase 11 does NOT remove this blob from `components.css` (risk of regression on other pages that may depend on the fallback).
Instead, `css/pages/contact.css` overrides `.contact-hero-section .con-hero-bg { background-image: url('/assets/images/contact-hero.jpg') }`.
The external image (123.1 KB, already in `assets/images/`) is now served instead of the blob on the contact page.
Full base64 removal from `components.css` is scheduled for Phase 16.

### Performance

| Metric | Value |
|---|---|
| Legacy Contact size | ~4.22 MB (part of monolithic index.html) |
| `contact/index.html` | **52.8 KB** |
| `css/pages/contact.css` | **~9.4 KB** |
| `js/pages/contact.js` | **0 KB** (NONE) |
| Base64 removed from HTML | 0 KB (base64 was in components.css, not in HTML) |
| Base64 effectively bypassed | ~90 KB blob in components.css no longer used on contact page via CSS override |
| External images referenced by contact page | 1 (`/assets/images/contact-hero.jpg` = 123.1 KB) |
| Image loading strategy | Above-fold hero — NOT lazy-loaded (LCP candidate) |
| Scripts on page | 4 (global, navigation, animations, active-nav) |

### Content Flags (Preserved from Legacy — Not Invented)

Contact information preserved from approved legacy source (`src/pages/contact.html` Phase 1 extract + `legacy/index-original.html`):
- WhatsApp: `wa.me/91XXXXXXXXXX` — PLACEHOLDER, not a real number. **Phase 21 client configuration item.**
- Email: `contact@kritexalabs.com` — Present in both Phase 1 extract and legacy source. Verified as approved content.
- Office: "Rajiv Gandhi IT Park, Hinjewadi Phase I, Pune – 411057" — from approved legacy source. Not independently verified.
- Response time: "Within 2–4 hours on business days" — from approved legacy source. Not guaranteed.
- Stats (30+ Businesses Served, <4h Avg Response, 5.0★ Client Rating) — marketing claims from approved legacy source. Not independently verified.
- Budget figures (₹25,000 / ₹75,000) in FAQ — from approved legacy source.

### Known Deferred Items

- Phase 21: Replace `wa.me/91XXXXXXXXXX` with verified client WhatsApp number
- Phase 21: Connect form to real backend/CMS endpoint (replace `action="#"`)
- Phase 16: Remove base64 blob from `css/components.css` `.con-hero-bg`
- Phase 17: Contact Open Graph image
- Phase 17: Contact structured data (if LocalBusiness data becomes verified)
- Phase 14: Real device responsive QA
- Phase 20: Formal accessibility audit

---



## [Phase 10] — Case Studies Page Rebuild

### Added

- Created `src/sections/case-studies/` directory — 5 section partial HTML files:
  - `hero.html` — Inner page hero (eyebrow, H1 "Case Studies", description, 4-stat strip)
  - `featured.html` — Featured case study `<article>` with Problem/Solution/Result flow
  - `studies.html` — Filter navigation (visual/non-functional) + 6 case study `<article>` cards in a 3-col grid
  - `faq.html` — 6 FAQ accordion items with `<button>` + `aria-expanded` + `aria-controls`
  - `cta.html` — Bottom CTA section (H2 + 2 CTAs → `/contact` and `/portfolio`)
- Created `css/pages/case-studies.css` — Case-studies-specific styles (~7 KB):
  - `.cs-hero-section` — hero section padding override
  - `.cs-hero-stats` / `.cs-hstat` / `.cs-hstat-num` / `.cs-hstat-lbl` / `.cs-hstat-div` — stats strip
  - `.cs-filter-btn` / `.cs-filter-active` — filter button styles with `aria-pressed` states
  - `.cs-title` — case study card heading variant
  - `.cs-cta-inner` / `.cs-cta-btns` — bottom CTA section
  - Responsive: 3-col → 2-col → 1-col grid; filter wrap; CTA stack at ≤768px
  - Reduced motion: `transition: none !important` on filter buttons
- Rewrote `src/pages/case-studies.html` — replaced Phase 1 monolithic content with section composition template (`{{SECTION:case-studies/*}}`)
- Updated `scripts/build.js`:
  - Version comment updated: "Phase 10 Build System"
  - `case-studies` page definition: title, description, activeNav updated; `css` array added (`/css/pages/case-studies.css`)

### Changed

- `src/pages/case-studies.html` — Replaced Phase 1 extract (legacy monolithic content with inline styles, onclick handlers) with clean section partial composition
- `scripts/build.js` — Build system version comment updated to Phase 10

### Architecture Decisions

- AD-044: Case Studies Section Partial Architecture (5 sections: hero, featured, studies, faq, cta)
- AD-045: Case Studies Cards — Non-Linked Preview Pattern
- AD-046: FAQ — Global JS Handler (no page-specific JS)
- AD-047: Filter Buttons — Visual Only in Phase 10 (no filter JS)

### Navigation Fixes (Case Studies page)

- `nl-case-studies` nav link correctly receives `class="active"` + `aria-current="page"` via `scripts/build.js` `injectActiveNav()`
- Mobile drawer `ml-case-studies` is not manipulated by build.js (active-nav.js handles this at runtime)

### JavaScript

**ZERO** page-specific JavaScript created for Case Studies.
FAQ accordion: handled by existing `js/global.js` `toggleFaq()` function.
Filter buttons: `aria-pressed` set statically (no filtering logic) — Phase 10 preserves visual appearance.

### Base64 Extraction

**None required** — the legacy case-studies section used CSS gradient thumbnails (`.cst-1`, `.cst-2`, `.cst-3`) for case study card backgrounds. No raster images were embedded in the case-studies content. Zero base64 blobs in generated `case-studies/index.html`.

### Performance

| Metric | Value |
|---|---|
| `case-studies/index.html` | 63.3 KB |
| `css/pages/case-studies.css` | ~7 KB |
| `js/pages/case-studies.js` | 0 KB (NONE) |
| Base64 removed | 0 KB (none was present) |
| External images added | 0 |
| Scripts on page | 4 (global, navigation, animations, active-nav) |

### Content Flags (Preserved from Legacy — Not Invented)

All case study metrics are marketing claims from the approved legacy source:
- "3× More Leads", "95 Lighthouse", "#1 Google Maps", "30d To Launch" (featured clinic)
- "₹2.5M Revenue", "10× Revenue", "4.8% CVR" (E-Commerce)
- "80% Auto-Qual", "5× Speed", "42% Close↑" (WhatsApp AI)
- "#1 Maps", "60d To Rank", "2.4× Traffic" (Local SEO Restaurant)
- "3× Calls", "90% Quality", "97 Score" (Finance Consultant)
- "2× Enroll", "85% Online", "4× Traffic" (Education)
- "5× Leads", "38% CVR", "96 Score" (Interior Design)
- Technologies (Next.js, Stripe, WhatsApp Bot, etc.) — from approved legacy source
None independently verified.

### Known Deferred Items

- Individual case study detail pages (`/case-studies/slug`) — future phase
- Filter functionality (JS filtering) — if required, future phase
- Real device responsive QA (Phase 14)
- Case Studies Open Graph image (Phase 17)
- Case Studies structured data — FAQPage, WebPage (Phase 17)
- Client verification and content review (Phase 21)

---

## [Phase 9] — Portfolio Page Rebuild

### Added

- Created `src/sections/portfolio/` directory — 4 section partial HTML files:
  - `hero.html` — Inner page hero (eyebrow, H1 "Projects That Deliver Results", description, stats strip with 4 animated counters)
  - `filters.html` — Category filter (9 buttons + All) + Type filter (3 tabs: All/Website/Software) with `aria-pressed`
  - `projects.html` — 12 project cards in 2-col grid; each is a semantic `<article>` with `aria-labelledby`, `data-category`, `data-type`
  - `cta.html` — Bottom CTA section (H2 + 2 CTAs → `/contact` and `/capabilities`)
- Created `css/pages/portfolio.css` — Portfolio-page-specific styles (~16 KB):
  - `.pf-hero-section` stats strip styles
  - `.pf-filter-sec`, `.pf-filter`, `.pf-type-tab` filter button styles
  - `.pf-grid` 2-col responsive grid
  - `.pf-card` layout overrides and image container
  - Category + tag color variant classes (cyan, purple, green, orange, yellow, pink, red, teal)
  - `.pf-view-btn` anchor-button style
  - `.pf-techs` list reset
  - `.sr-only` utility
  - Responsive breakpoints for 360px, 414px, 600px, 768px, 900px, 1024px
  - Reduced-motion overrides for all portfolio animations
- Created `js/pages/portfolio.js` — Portfolio-specific JavaScript (~8 KB):
  - Stat counter animation (IntersectionObserver, cubic ease-out, 1600ms, prefers-reduced-motion aware)
  - Filter logic: category + type cross-filtering, `.active` + `aria-pressed` management
  - Count text live region update (`aria-live="polite"`)
  - `data-count` attribute removal guard (prevents global.js counter conflict)
- Extracted `assets/images/portfolio-placeholder.jpg` (73.4 KB) — single image used for all 12 portfolio cards in legacy source
- Updated `src/pages/portfolio.html` — replaced Phase 1 extracted monolith (with inline base64 and onclick handlers) with Phase 9 semantic section composition
- Added `css` and `js` arrays to portfolio page definition in `scripts/build.js`
- Updated `scripts/build.js`:
  - Added `pageJs` parameter to `buildPageWrapper()` — injects page-specific `<script>` tags after `active-nav.js`
  - Updated label to "Phase 9 Build System"
- Updated portfolio page `title` and `description` in `scripts/build.js`:
  - Title: `Portfolio — Kritexa Labs | Our Work`
  - Description: accurate page-specific description

### Changed

- `src/pages/portfolio.html` — completely replaced Phase 1 extracted content with Phase 9 semantic composition
- `scripts/build.js` — extended build system with `page.js` support; build label updated to Phase 9
- All portfolio page CTAs converted from `onclick="go('contact')"` `<button>` to `<a href="/contact">` anchors:
  - All 12 "View Project" buttons → `<a class="pf-view-btn" href="/contact">Start a Similar Project</a>`
  - Hero section has no link CTA (informational page) ← intentional — stats strip is informational
- Portfolio card structure upgraded:
  - `<div class="pf-card">` → `<article class="pf-card rv" role="listitem" aria-labelledby="pf-title-NN">`
  - Technology lists: `<div class="pf-techs">` → `<ul class="pf-techs"><li class="pf-tech">` with `aria-label`
  - Category + type tags: inline `style=` → semantic CSS class variants (`pf-cat-cyan`, `pf-tag-purple`, etc.)
  - Count bar text updated dynamically via `js/pages/portfolio.js` (was static in legacy)
- Filter buttons: `onclick="pfFilter(this,'cat','all')"` removed → event delegation on parent containers
- Filter button `aria-pressed` attributes managed by portfolio.js (not hardcoded after first click)

### Architecture Decisions

- **AD-039** — Portfolio section partial architecture: mirrors Phase 6/7/8 pattern. Four partials in `src/sections/portfolio/`. See ARCHITECTURE-DECISIONS.md.
- **AD-040** — Portfolio filter JavaScript: Filter system from approved legacy source preserved. Uses event delegation (not inline onclick) for cleaner separation. `data-filter-type` and `data-filter-value` data attributes replace inline function call parameters. The filter is the only non-CSS functionality on the Portfolio page.
- **AD-041** — Portfolio CTA text: Legacy `<button>View Project</button>` onclick was changed to `<a href="/contact">Start a Similar Project</a>`. "Start a Similar Project" is more accurate than "View Project" since no individual project pages exist. CTA destination: `/contact` per AD-019.
- **AD-042** — Portfolio placeholder image: The legacy source used one identical base64 JPEG for all 12 portfolio cards. Phase 9 extracts this as `portfolio-placeholder.jpg` and uses it for all 12 cards. Individual project screenshots are future work (Phase 16 or later, when real client project assets are approved).
- **AD-043** — build.js `js` array: The existing `css` array pattern in build.js was extended to support a `js` array for page-specific scripts. This allows portfolio.js (and future page scripts) to be cleanly declared in the page definition without modifying buildPageWrapper logic for each new page.

### Navigation Fixes (Portfolio page)

| Legacy | New | Status |
|--------|-----|--------|
| `onclick="go('contact')"` button | `href="/contact"` anchor | ✅ Fixed |
| `onclick="pfFilter(this,'cat','Healthcare')"` inline | `data-filter-value="Healthcare"` + JS listener | ✅ Fixed |

### JavaScript

Portfolio-specific JavaScript in `js/pages/portfolio.js`:
- Filter system (category + type cross-filter, aria-pressed, count text)
- Stat counter animation (IntersectionObserver, requestAnimationFrame, reduced motion)
- Global scripts: global.js, navigation.js, animations.js, active-nav.js

### Base64 Extraction

| Blob | Description | Extracted Path | Size |
|------|-------------|----------------|------|
| Portfolio cards (×12 identical) | Shared placeholder image | `assets/images/portfolio-placeholder.jpg` | 73.4 KB |

**Note**: All 12 portfolio cards in the legacy source used the same base64 JPEG (100,192 chars = ~73.4 KB decoded). This is a confirmed placeholder — no unique project screenshots exist in the approved source.

### Performance

| Metric | Legacy (Phase 1 extract) | Phase 9 |
|--------|--------------------------|---------|
| `portfolio/index.html` size | ~1.7 MB (with 12×base64 blobs) | **78.0 KB** |
| Base64 images in portfolio HTML | ~870 KB (12 × same blob) | **0 KB** |
| Portfolio-specific CSS | 0 KB | ~16 KB (`css/pages/portfolio.css`) |
| Portfolio-specific JS | 0 KB | ~8 KB (`js/pages/portfolio.js`) |
| External images referenced | 0 | 1 (`portfolio-placeholder.jpg` = 73.4 KB) |
| Base64 removed from HTML | — | ~870 KB (95%+ size reduction) |

### Content Flags (Preserved from Legacy — Not Invented)

- All 12 project names, descriptions, results, and technology stacks preserved from approved `src/pages/portfolio.html` content
- All project results (3×, #1, 80%, ₹2Cr, etc.) are from original marketing content — not independently verified
- "SEBI Compliant" claim on FinVault preserved from approved content
- "₹2Cr Seed Raised" on LaunchPad SaaS is marketing claim from original source
- All project dates are 2026 — consistent with "Est. 2026" company founding
- "Arjun Mehta" and "Priya Sharma" are project/client names from approved source — not independently verified as real clients
- All technology stacks preserved from original source — not independently verified
- `portfolio-placeholder.jpg` is a generic stock photo used for all 12 cards — individual project images do not exist in approved assets

### Known Deferred Items

- Real device responsive QA — Phase 14
- Portfolio Open Graph image — Phase 17
- Individual project screenshots/images — future phases when actual project assets approved
- Portfolio structured data (`ItemList`, `CreativeWork`) — Phase 17
- Individual case study pages (`/case-studies/[slug]`) — future phase (not Phase 9)
- WebP conversion of portfolio-placeholder.jpg — Phase 16
- Client name verification (Arjun Mehta, Priya Sharma) — Phase 21 content review

---


## [Phase 8] — Capabilities Page Rebuild

### Added

- Created `src/sections/capabilities/` directory — 4 section partial HTML files:
  - `hero.html` — Inner page hero (eyebrow, H1 "Our Full Capabilities", description, CTA)
  - `services.html` — Hero card "Business Growth Engine" + 6 capability cards in two rows of 3
  - `who-we-serve.html` — 8-industry grid (Healthcare → Personal Brands)
  - `industry-solutions.html` — Decorative divider pill + 8 alternating industry solution rows
- Created `css/pages/capabilities.css` — Capabilities-page-specific styles:
  - `.sr-only` utility (visually-hidden section headings for screen readers/SEO)
  - Link-reset for `a.cap-svc-card` (semantic anchors, not `<div onclick>`)
  - `a.sol-btn` anchor reset
  - `.cap-industry-divider` / `.cap-divider-pill` pill separator styles
  - `.sol-feats` list reset (upgraded from `<div>` to `<ul>`)
  - `.cap-card-svcs` / `.cap-hc-svcs` list resets
  - Responsive breakpoints for 360px, 480px, 640px, 768px, 1024px
  - Reduced-motion overrides for `.cap-industry-tile` hover
- Updated `src/pages/capabilities.html` — completely replaced Phase 1 extracted content (1,602 KB monolith with `onclick="go()"`) with Phase 8 semantic section composition
- Extracted 4 new capability solution images from base64 blobs:
  - `assets/images/solution-05.jpg` (144.7 KB — Local Business)
  - `assets/images/solution-06.jpg` (192.5 KB — Education Institutes)
  - `assets/images/solution-07.jpg` (181.8 KB — E-Commerce & D2C)
  - `assets/images/solution-08.jpg` (113.8 KB — Personal Brands)
- Updated `scripts/asset-manifest.json` — documented Phase 8 image extraction
- Added `css: ['/css/pages/capabilities.css']` to capabilities page definition in `scripts/build.js`
- Updated capabilities page `title` and `description` in `scripts/build.js`:
  - Title: `Capabilities — Kritexa Labs | Websites, AI, Automation & Growth`
  - Description: accurate page-specific description

### Changed

- `src/pages/capabilities.html` — completely replaced Phase 1 extracted content with Phase 8 semantic composition
- `scripts/build.js` — build system label updated to "Phase 8"; capabilities page gets `css` array
- All capabilities page navigation converted from `onclick="go('...')"` `<button>`/`<div>` to `<a href>` anchors:
  - Hero CTA "Get Free Consultation →" → `/contact`
  - All 6 capability cards → `/contact`
  - Hero card arrow → `/contact`
  - All 8 industry solution "Get This Solution →" buttons → `/contact`
- Capability cards upgraded from `<div onclick>` to `<a href="/contact">` with correct semantics
- Service checklists upgraded from `<div class="cap-card-svc">` to `<ul>/<li>` with `aria-label`
- Industry feature lists upgraded from `<div class="sol-feat">` to `<ul>/<li>` with `aria-label`
- Industry tiles upgraded from `<div>` to `<li role="listitem">` within `<ul role="list">`
- Industry solution image rows use `<article aria-labelledby>` for semantic grouping

### Architecture Decisions

- **AD-035** — Capabilities section partial architecture: mirrors Phase 6/7 pattern. Four partials (`hero`, `services`, `who-we-serve`, `industry-solutions`) in `src/sections/capabilities/`. See ARCHITECTURE-DECISIONS.md.
- **AD-036** — Capabilities card-as-link: service cards are `<a href="/contact">` links. Capability cards are not navigation but conversion CTAs — a full-card anchor is appropriate for touch usability. The `role="list"` / `role="listitem"` on the grid preserves list semantics.
- **AD-037** — Industry solutions heading level: Each sol-row uses `<h2>` (not `<h3>`) because the 8 industry headings are major page sections in their own right, at the same semantic level as "Built for Real Businesses" and section labels. The page has one H1 → multiple H2s → H3s inside service section.
- **AD-038** — Solution images: solution-01 is NOT lazy-loaded (it is the first visible image in the industry solutions section, which could be LCP on slower connections). Solutions 02–08 use `loading="lazy"`. Width/height attributes set to `600×450` to reduce CLS risk.

### Navigation Fixes (Capabilities page)

| Legacy | New | Status |
|--------|-----|--------|
| `onclick="go('contact')"` button | `href="/contact"` anchor | ✅ Fixed |
| `<div class="cap-hc-arrow" onclick>` | `<a class="cap-hc-arrow" href="/contact">` | ✅ Fixed |
| `<div class="cap-svc-card" onclick>` | `<a class="cap-svc-card" href="/contact">` | ✅ Fixed |
| `<button class="sol-btn" onclick>` | `<a class="sol-btn" href="/contact">` | ✅ Fixed |

### JavaScript

**ZERO Capabilities-specific JavaScript.** No `js/pages/capabilities.js` created.
- Global scroll reveal (`.rv`): `js/global.js` IntersectionObserver
- Navigation, mobile menu: `js/navigation.js`
- Active nav state: `js/active-nav.js`
- No canvas, no countdown, no orbital needed on Capabilities.

### Base64 Extraction

| Blob | Industry | Extracted Path | Size |
|------|----------|----------------|------|
| 1 | Clinics & Healthcare | `assets/images/solution-01.jpg` | 144.7 KB (Phase 1) |
| 2 | Consultants & Coaches | `assets/images/solution-02.jpg` | 127.0 KB (Phase 1) |
| 3 | Finance & Advisors | `assets/images/solution-03.jpg` | 132.4 KB (Phase 1) |
| 4 | Startups & Scale-Ups | `assets/images/solution-04.jpg` | 146.9 KB (Phase 1) |
| 5 | Local Businesses | `assets/images/solution-05.jpg` | 144.7 KB (**Phase 8**) |
| 6 | Education Institutes | `assets/images/solution-06.jpg` | 192.5 KB (**Phase 8**) |
| 7 | E-Commerce & D2C | `assets/images/solution-07.jpg` | 181.8 KB (**Phase 8**) |
| 8 | Personal Brands | `assets/images/solution-08.jpg` | 113.8 KB (**Phase 8**) |

**Note**: Phase 1 had extracted 7 images but capabilities needed 8. Images 5–8 were written by Phase 8. The Phase 1 solution-05/06/07 files were overwritten with the correct capabilities images.

### Performance

| Metric | Legacy (Phase 1 extract) | Phase 8 |
|--------|--------------------------|---------|
| `capabilities/index.html` size | 1,602 KB (base64 monolith) | **65.8 KB** |
| Base64 images in capabilities HTML | 1,184 KB (8 blobs) | **0 KB** |
| Capabilities-specific CSS | 0 KB | ~7.5 KB (`css/pages/capabilities.css`) |
| Capabilities-specific JS | 0 KB | 0 KB (none needed) |
| External images referenced | 0 | 8 (`/assets/images/solution-*.jpg`) |
| Base64 removed from HTML | — | 1,184 KB (96% size reduction) |

### Known Deferred Items

- Real device responsive QA — Phase 14.
- Capabilities Open Graph image — Phase 17.
- Capabilities structured data (Service schema) — Phase 17 (explicitly deferred per scope rules).
- sol-row image `alt` text quality — Phase 17 (text is functional; no client-specific imagery to reference).
- Legacy base64 blobs 5-8 remain in `css/components.css` or `legacy/index-original.html` — superseded and not rendered. Phase 16.
- Phase 1 solution-05/06/07 files were overwritten. The original Phase 1 files for those slots were lower-resolution extracts from a different extraction run. Phase 16 WebP conversion will replace all with optimized formats.

### Content Flags (Preserved from Legacy — Not Invented)

- Marketing language "#1 on Google Maps" and "fill your schedule automatically" preserved from approved content
- "SEBI/IRDAI compliant" claim preserved from approved content — not verified by Kritexa Labs team
- Statistics in hero (50+/30+/8) are present in GCTA/footer — capabilities page hero has no stat strip (correct — hero stats are on About/Home pages)

---


## [Phase 7] — About Page Rebuild

### Added

- Created `src/sections/about/` directory — 3 section partial HTML files:
  - `hero.html` — Full-screen rounded hero (bg image, H1, kicker, stats, CTAs, floating badges)
  - `mission-vision.html` — Two-card grid (Mission + Vision `.b-card` articles)
  - `values.html` — Section heading + 6 value cards (`.val-grid`)
- Created `css/pages/about.css` — About-page-specific styles:
  - `.sr-only` utility (for visually-hidden section headings)
  - `.about-mv-section` / `.about-mv-grid` / `.about-mv-card` layout
  - `.about-fs-hero-bg` override: replaces legacy base64 with `/assets/images/about-hero.jpg`
  - `.about-fs-btns` anchor reset (`.btn-p`/`.btn-g` as `<a>` elements)
  - Responsive breakpoints for 360px, 414px, 768px
  - Reduced-motion overrides for `about-fs-scroll`, `about-fs-badge`, `val-card`
- Updated `src/pages/about.html` — new composition model using `{{SECTION:about/*}}` partials
  - Replaced legacy Phase 1 extract (inline monolith with `onclick="go()"` buttons)
  - Now uses `<main id="main-content">` landmark
  - Three section partials composed at build time
- Added `css: ['/css/pages/about.css']` to About page definition in `scripts/build.js`
- Updated About page `title` and `description` in `scripts/build.js`:
  - Title: `About Kritexa Labs — AI-First Digital Growth Studio, Pune`
  - Description: accurate page-specific description

### Changed

- `src/pages/about.html` — completely replaced Phase 1 extracted content with Phase 7 semantic composition
- `scripts/build.js` — build system label updated to "Phase 7"; About page gets `css` array
- All About page CTAs converted from `onclick="go('...')"` `<button>` to `<a href>` anchors:
  - "Work With Us →" → `/contact`
  - "Join the Team ↗" → `/career`

### Architecture Decisions

- **AD-032** — About section partial architecture: mirrors Phase 6 home section pattern. Three partials (`hero`, `mission-vision`, `values`) in `src/sections/about/`. See ARCHITECTURE-DECISIONS.md.
- **AD-033** — About hero background image: `css/pages/about.css` overrides the `background-image` in `css/components.css` (which retains legacy base64 for backward compat) with `url('/assets/images/about-hero.jpg') !important`. The `!important` is justified because `components.css` uses an inline base64 blob that cannot be removed until Phase 16 (Performance Optimization).
- **AD-034** — About H1 strategy: The H1 is a real visible HTML text element ("We Think in Systems, Not Pages") — no canvas trickery needed. The grad-text span clips gradient to `<span>` text only.

### Navigation Fixes (About page)

| Legacy | New | Status |
|--------|-----|--------|
| `onclick="go('contact')"` button | `href="/contact"` anchor | ✅ Fixed |
| `onclick="go('career')"` button | `href="/career"` anchor | ✅ Fixed |

### JavaScript

**ZERO About-specific JavaScript.** No `js/pages/about.js` created.
- Global scroll reveal (`.rv`): `js/global.js` IntersectionObserver
- Navigation, mobile menu: `js/navigation.js`
- Active nav state: `js/active-nav.js`
- No canvas, no countdown, no orbital needed on About.

### Performance

| Metric | Legacy (Phase 1 extract) | Phase 7 |
|--------|--------------------------|---------|
| `about/index.html` size | ~46 KB (legacy extract, base64 bg in CSS) | 45.5 KB |
| Base64 images in about HTML | 0 (already in CSS) | 0 |
| Base64 in CSS (components.css) | Yes (hero bg) | Overridden by about.css `!important` |
| About-specific CSS | 0 KB | ~5 KB (`css/pages/about.css`) |
| About-specific JS | 0 KB | 0 KB (none needed) |

**Note**: The legacy `about-fs-hero-bg` base64 blob remains in `css/components.css` (Phase 0 remnant). It is overridden by `css/pages/about.css`. Full removal of the base64 blob from `components.css` is a Phase 16 (Performance) task.

### Known Deferred Items

- `css/components.css` line ~1353: legacy base64 blob on `.about-fs-hero-bg` — overridden by `about.css` but not removed. Phase 16.
- Val-grid responsive QA on real devices — Phase 14.
- About page Open Graph image — Phase 17.
- About page structured data (Organization, LocalBusiness) — Phase 17 (explicitly deferred per Phase 7 scope).
- About CTA "Work With Us →" destinations to `/contact` — permanent destination, approved.
- About CTA "Join the Team ↗" destinations to `/career` — permanent destination, approved.

---


## [Phase 6] — Home Page Rebuild

### Added

- Created `src/sections/home/` directory — 8 section partial HTML files:
  - `hero.html` — BUSINESS canvas hero, CTAs, scroll hint
  - `marquee.html` — Industry trust marquee (10 industries, duplicated for loop)
  - `services.html` — "What We Build" hero card + 6 service cards
  - `why-us.html` — Bento "Why Us" grid (AI First, Results, Speed, Metrics, Scale)
  - `process.html` — 7-step process (desktop horizontal + mobile orbital)
  - `stats.html` — 4 animated stat counter boxes
  - `case-studies-preview.html` — 3 case study preview cards
  - `testimonials.html` — Auto-scrolling testimonials (6 cards, duplicated for loop)
- Created `js/pages/home.js` — Home-specific JavaScript:
  - BUSINESS text glow canvas animation (from `js/animations.js`, isolated)
  - Process desktop spine positioning + mobile orbital SVG layout
  - Stat counter animation with "+" suffix
  - `prefers-reduced-motion` awareness
  - Canvas pauses when page is hidden (`visibilitychange`)
- Populated `css/pages/home.css` — Home-specific overrides:
  - `.jk-sr-h1` visually-hidden H1 pattern
  - `.cs-card-link` / `a.cap-svc-card` link resets for semantic navigation
  - Reduced-motion overrides for `jk-orb` and `jk-scroll-hint`
  - Mobile refinements for 320–360px
  - Section scroll-margin-top for sticky navbar offset
- Updated `src/pages/home.html` — new composition model using `{{SECTION:home/*}}` partials
- Added `{{SECTION:path}}` partial resolution to `scripts/build.js`
- Added `css` array field to page definitions in `scripts/build.js` for page-specific stylesheets
- Added `/css/pages/home.css` injection to `index.html` (home page only)

### Changed

- `src/pages/home.html` — completely replaced legacy content extraction (SPA-era) with new semantic section composition
- `scripts/build.js` — added `SRC_SECTIONS` constant, `{{SECTION:*}}` processing, `pageCss` parameter in `buildPageWrapper`, updated console label to "Phase 6 Build System"
- All Home page CTAs converted from `onclick="go('...')"` `<button>` to `<a href="/...">` anchors:
  - "Start Your Project →" → `/contact` (temporary per AD-019)
  - "View Our Work ↗" → `/portfolio`
  - Service cards + hero card → `/capabilities`
  - "View All Case Studies →" → `/case-studies` (was `go('blog')`)
- Hero section: added visually-hidden H1 (`<h1 class="jk-sr-h1" id="home-h1">`)
- Hero section: added `aria-labelledby="home-h1"` to `<section class="jk-hero">`
- Hero CTA buttons converted from `<button onclick>` to `<a href>`
- Testimonials converted from `<div class="t-card">` to `<blockquote>` with `<footer>`
- Testimonial author roles updated from underscore format to human-readable (e.g. `clinic_owner · Pune` → `Clinic Owner · Pune`)
- Case study cards wrapped in `<article>` with `<a href="/case-studies">` for accessible navigation
- Progress bars in Why Us section got `role="progressbar"` and `aria-valuenow/min/max`
- Bento grid got `role="list"` / `role="listitem"`

### Architecture Decisions

- **AD-029** — Home sections are independently maintained HTML partials in `src/sections/home/`. The build system inlines them at compile time. No runtime HTML injection.
- **AD-030** — Home-specific JS (`js/pages/home.js`) isolates canvas animation and process orbital code from global `js/animations.js`. The `js/animations.js` file remains loaded on all pages for non-home functionality (countdown timer, etc.). Canvas animation in `js/animations.js` falls back gracefully on the home page because `jkCanvas` still exists — the animation runs in both; home.js takes precedence as it loads after. *Follow-up for Phase 15*: Add a guard in `js/animations.js` to skip BUSINESS canvas on home page, since `js/pages/home.js` handles it.
- **AD-031** — H1 strategy for Home: The visual "BUSINESS" text is canvas-rendered and aria-hidden. A visually-hidden `.jk-sr-h1` provides the semantic H1 for SEO and accessibility. This satisfies exactly-one-H1 requirement without altering the approved visual design.

### Navigation Fixes (Home page)

| Legacy | New | Status |
|--------|-----|--------|
| `onclick="go('contact')"` | `href="/contact"` | ✅ Fixed |
| `onclick="go('portfolio')"` | `href="/portfolio"` | ✅ Fixed |
| `onclick="go('capabilities')"` | `href="/capabilities"` | ✅ Fixed |
| `onclick="go('blog')"` | `href="/case-studies"` | ✅ Fixed |

### Known Deferred Items

- `js/animations.js` BUSINESS canvas still loads on home page — canvas duplication risk. Fix in Phase 15 by adding `if (document.querySelector('meta[name="page-id"][content="home"]'))` guard.
- Hero visually-hidden H1 text ("Kritexa Labs — AI-First Digital Growth Studio") duplicates the `<title>` tag. This is intentional and acceptable.
- `js/animations.js` countdown timer still references `kaiDays/kaiHrs/kaiMins/kaiSecs` elements — safely no-ops on home page (elements not present).
- Stats "+" suffix implementation assumes stat counters are not already animated by `js/global.js`. The `js/pages/home.js` removes `data-count` from `.stat-box-val` before `js/global.js` can act on them (load order: `global.js` loads first, but IntersectionObserver callback fires asynchronously, and `home.js` removes the attribute synchronously at parse time). Confirm behavior in Phase 15.

### Performance

| Metric | Legacy | Phase 6 |
|--------|--------|---------|
| `index.html` size | 4,220 KB (base64 monolith) | 81 KB |
| Home-specific CSS | 0 KB (global-only) | 4.5 KB |
| Home-specific JS | 0 KB (global animations.js) | 17.5 KB |
| Base64 images in home HTML | ~3 MB | 0 KB |
| External images | 0 | 0 (none needed by Home page sections) |
| Section count | 1 monolithic file | 8 section partials |

---

## [Phase 5] — Global Footer

### Added
- Created `css/footer.css` — dedicated footer stylesheet using Phase 2 design tokens. Contains all `.gcta`, `.footer`, `.f-nl-*`, `.f-main-grid`, `.f-links-*`, `.f-col-*`, `.f-copyright-strip`, `.f-brand-*`, `.f-socials`, `.f-soc`, `.f-powered-*`, `.f-link-*`, `.f-link-pending`, `.btn-wa`, `.btn-email`, focus states, responsive breakpoints, and reduced-motion overrides.
- Added Phase 5 footer navigation — all 9 real production routes available via footer `<a href>` anchors.
- Added `aria-label` attributes to all 6 social icon links (`aria-label="Kritexa Labs on [Platform]"`).
- Added `role="list"` / `role="listitem"` to `.f-socials` for semantic grouping.
- Added `aria-labelledby` on all 4 footer navigation `<ul>` elements linked to their column headings.
- Added `<nav aria-label="Footer navigation">` wrapping `.f-links-inner`.
- Added `.f-link-pending` class — visually muted non-clickable `<span>` for non-existent legal pages.
- Added `.f-link-kai` (cyan) and `.f-link-labs` (purple2) for identity-consistent link colors.
- Added `.sr-only` helper class in `css/footer.css` for newsletter label.
- Added `focusable="false"` on all social SVG elements.
- Added explicit `aria-hidden="true"` on `f-powered-sep` divider.

### Changed
- Converted newsletter `onclick="alert(...)"` to a standard `<form action="#" method="post">` with `<label>`, `type="email"`, `autocomplete="email"`, `required`, and `type="submit"` (AD-027).
- Removed duplicate "Services" link from Main Pages column — was duplicating Capabilities.
- Replaced dead `<a href="/privacy-policy">`, `<a href="/terms-of-service">`, `<a href="/cookie-policy">` links with `<span class="f-link-pending">` placeholders (AD-026).
- Updated copyright year from `2026` to `2025`.
- Updated `f-brand-logo` from using inline `style=` override to proper class-level CSS in `css/footer.css`.
- Updated social icon `title=""` attributes to `aria-label=""` (accessible text takes precedence).
- Added `loading="lazy"` to footer logo image (below-fold asset).
- Added `alt=""` (decorative) to footer logo image; accessible text provided by `aria-label` on the parent `<a>`.
- Updated `scripts/build.js` CSS load order to include `css/footer.css` (between `header.css` and `components.css`).
- Updated build console header to "Phase 5 Build System".

### Architecture Decisions
- **AD-026** — Legal pages (`/privacy-policy`, etc.) deferred to Phase 21. Non-clickable placeholders used in footer.
- **AD-027** — Newsletter form uses standard HTML `<form>` — no `onclick` JavaScript.
- **AD-028** — Footer CSS lives in `css/footer.css`; load order: `global → utilities → header → footer → components`.

### Social Links (Verified)
All 6 social links preserved from `legacy/index-original.html`:
- Instagram: `https://instagram.com/kritexalabs`
- X/Twitter: `https://x.com/kritexalabs`
- LinkedIn: `https://www.linkedin.com/company/kritexa-labs/`
- Facebook: `https://www.facebook.com/kritexalabs1`
- YouTube: `https://youtube.com/@kritexalabs`
- Threads: `https://threads.net/@kritexalabs`

### Contact Information
- Email: `hr@kritexalabs.com` (preserved from existing GCTA section)
- WhatsApp: `91XXXXXXXXXX` — placeholder preserved; real number to be added in Phase 21. No fake number invented.

### Legal Pages Status
- `/privacy-policy` — **does not exist** — placeholder in footer
- `/terms-of-service` — **does not exist** — placeholder in footer
- `/cookie-policy` — **does not exist** — placeholder in footer

### Dependencies Added
- **Zero.** No new libraries, frameworks, icon packages, or animation packages introduced.

### Deferred to Later Phases
- Real newsletter email service endpoint (Phase 21)
- Real WhatsApp number in wa.me link (Phase 21)
- Legal page content + links (Phase 21)
- Individual page content updates (Phases 6–13)

### Build Result
```
✓ index.html
✓ about/index.html
✓ capabilities/index.html
✓ portfolio/index.html
✓ case-studies/index.html
✓ contact/index.html
✓ kritexa-ai/index.html
✓ career/index.html
✓ labs/index.html
All 9 pages built with footer + footer.css reference.
```

---

## [Phase 4] — Products Mega Menu

### Added
- Implemented full Products mega menu in `components/header.html` — replaces Phase 3 placeholder.
- Built `components/mega-menu.html` — full reference implementation with all four approved categories.
- Added four product category columns to desktop mega menu: **Solutions**, **Tools**, **APIs**, **Platforms**.
- Each category has: icon (inline SVG, `stroke="currentColor"`), title, structural description, and "Coming Soon" badge.
- Added `pmm-footer-strip` — bottom strip with Kritexa.AI cross-link (the only real navigable link in the menu).
- Added animated cyan dot (`pmm-footer-dot`) reusing `@keyframes kai-blink` from Phase 3.
- Added mobile Products accordion in `.nav-mob` — 2-column grid of category items with icons and "soon" badges.
- Added `closeMobProducts()` function to `js/navigation.js` — extracted mobile Products close logic.
- Added `focusout` handler on desktop mega menu — automatically closes when focus leaves the menu.

### Changed
- Extended `css/header.css` with all Phase 4 `pmm-*` (desktop mega menu) and `mob-pmm-*` (mobile panel) CSS classes.
- Changed `.products-mega-menu` from `display:none/block` toggle to `opacity + transform + visibility` animation (AD-025).
- Updated `js/navigation.js` — Phase 4 mega menu open/close/keyboard/focusout behavior; `closeMob()` now calls `closeMobProducts()` to reset accordion when drawer closes.
- Updated `scripts/build.js` — JS load comment updated to reflect Phase 4.
- Improved Escape key handling — now checks desktop Products menu first, then mobile accordion (Escape on accordion closes panel without closing whole drawer), then mobile drawer.

### Responsive Added
- `@media (max-width: 1024px) and (min-width: 769px)` — mega menu switches to 2-column grid.
- `@media (max-width: 768px)` — desktop mega menu hidden with `display: none !important`; mobile accordion used instead.
- `@media (max-width: 360px)` — mobile accordion collapses to 1-column.

### Reduced Motion Added
- `products-mega-menu` transition durations shortened to `0.01ms` under `prefers-reduced-motion: reduce`.
- `transform: none !important` suppresses slide animation under reduced motion.
- `pmm-footer-dot` animation disabled under reduced motion.
- Functionality fully preserved under reduced motion.

### Architecture Decisions
- **AD-024** — Product category labels are intentional non-navigational labels (no `href`). Product destination pages are future work.
- **AD-025** — Mega menu uses `opacity + transform + visibility` pattern instead of `display:none/block`.

### Product Destination Status
> Product destination pages have **not** been implemented in Phase 4. The four category blocks are visual/structural labels only. Actual product URLs will be connected when product pages are created.

### Dependencies Added
- **Zero.** No new libraries, frameworks, icon packages, or animation packages introduced.

### Deferred to Later Phases
- Actual product page content and URLs (future phase)
- Footer rebuild (Phase 5)
- Page content updates (Phases 6–13)

---

## [Phase 3] — 2025-02-xx

### Added
- Created `css/header.css` — dedicated header and navigation stylesheet using Phase 2 design tokens. Contains all navbar, mobile drawer, Products trigger, Kritexa.AI pill, CTA, hamburger, skip-nav, scroll state, active state, focus state, and responsive styles.
- Created new `components/header.html` — fully rebuilt global header with semantic HTML structure, ARIA attributes, Products trigger button, skip navigation, mobile drawer with Products accordion placeholder, `aria-hidden` on decorative elements.
- Added skip navigation link (`<a href="#main-content" class="skip-nav">`) for keyboard accessibility.
- Added Products navigation trigger (`<button aria-expanded aria-controls aria-haspopup>`) — Phase 3 trigger only, Phase 4 will populate the mega menu.
- Added `aria-current="page"` to active navigation elements — applied at both build time (via `scripts/build.js`) and runtime (via `js/active-nav.js`).
- Added body scroll lock (`body.nav-open { overflow: hidden }`) while mobile menu is open.
- Added ARIA roles and labels to both nav regions, hamburger button, Products triggers.
- Added `products-mega-menu` container (empty Phase 4 placeholder with correct `aria-hidden`).
- Added 44px minimum touch targets on hamburger button and mobile navigation links.
- Added `@media (prefers-reduced-motion: reduce)` support in `css/header.css`.

### Changed
- Rebuilt `js/navigation.js` — replaced legacy Phase 1 SPA shim with Phase 3 navigation module: mobile menu open/close/toggle, body scroll lock, Escape key handling, outside-click handling, Products trigger state, hamburger ARIA sync, navbar scroll state, dynamic collapse. Legacy `go()` shim preserved inside guard for backward compatibility.
- Updated `js/active-nav.js` — added `aria-current="page"` support on both desktop and mobile links; restructured navMap to separate desktop/mobile IDs.
- Updated `js/global.js` — removed navbar scroll, hamburger, and dynamic collapse code (moved to `js/navigation.js`).
- Updated `scripts/build.js` — added `css/header.css` to page wrapper stylesheet order; updated `injectActiveNav()` to correctly handle new HTML attribute ordering and inject `aria-current="page"`; added `wrapMainContent()` for skip-nav support; updated JS load order comments.
- Updated `css/components.css` — removed duplicate navbar CSS (now in `css/header.css`); removed duplicate nav-kai-pill CSS; removed duplicate nav-collapsed responsive CSS; removed duplicate 768px navbar media query.

### Navigation Routes (Clean URLs)
All navigation links in the new global header use clean multi-page URLs:
- `/` — Home
- `/about` — About
- `/capabilities` — Capabilities
- `/portfolio` — Portfolio
- `/case-studies` — Case Studies
- `/contact` — Contact
- `/kritexa-ai` — Kritexa.AI
- `/career` — Career (mobile drawer + footer only)
- `/labs` — Labs (mobile drawer + footer only)

### Deferred to Later Phases
- Products mega menu content (Phase 4)
- Footer rebuild (Phase 5)
- Page content updates — `onclick="go(...)"` remaining in page templates (Phases 6–13)
- Main content `<main>` landmark on individual pages (Phases 6–13)

---

## [Phase 2] — 2025-02-15

### Added
- Created a centralized design token system in `css/tokens.css` capturing all color palette, semantic, typography, spacing, border, radius, shadow, glow, z-index, and motion values.
- Established backward compatibility aliases inside `:root` variables to map all legacy selectors to the new tokens without specificity or layout regression risks.
- Formulated global accessibility foundations: `:focus-visible` outline treatments with offsets and `@media (prefers-reduced-motion: reduce)` media query triggers (resetting transition states and disabling decorative atmospheric blobs and cursor rings).

### Cleaned & Optimized
- Re-structured CSS architectures with clear one-way dependency order: `tokens.css` → `global.css` → `components.css` → `utilities.css`.
- Eliminated over 300 lines of duplicate style declarations, resets, and container rules originally duplicated between `css/global.css`, `css/utilities.css`, and `css/components.css`.
- Synchronized `.ab1`, `.ab2`, and `.ab3` ambient blobs directly with global ambient gradients.
- Validated all static templates compiled correctly, checking index layout dimensions to ensure 100% visual regression equivalence.

### Deferred
- Re-building of navigation header, dropdown layouts, and individual inner page contents (deferred to subsequent development phases per project specification).

---

## [Phase 1] — 2025-02-10

### Added
- Created modular multi-page static architecture.
- Built a custom HTML partial composition build system (`scripts/build.js`) with zero third-party npm dependencies.
- Extracted 10 high-resolution images from base64 encoding to `/assets/` directory.
- Separated monolithic inline CSS into modular stylesheet folders: `css/global.css`, `css/utilities.css`, and `css/components.css`.
- Preserved legacy SPA routing shims for backward compatibility.
